import { css } from '../../stitches.config';

export const header = css({
  background: '$gray800',
  display: 'flex',
  justifyContent: 'center',
  padding: '1.25rem 0',

  img: {
    height: '2rem',
  },
});
